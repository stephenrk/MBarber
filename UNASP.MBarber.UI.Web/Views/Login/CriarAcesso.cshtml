@using UNASP.MBarber.UI.Web.ViewModels
@model RegisterViewModel
@{
    ViewBag.Title = "Novo acesso";
    Layout = null;
}

<!DOCTYPE html>
<html>


<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>MBarber | Login</title>

    <link href="~/Content/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/Content/font-awesome/css/font-awesome.css" rel="stylesheet">
    <link href="css/plugins/iCheck/custom.css" rel="stylesheet">

    <link href="~/Content/css/animate.css" rel="stylesheet">
    <link href="~/Content/css/style.css" rel="stylesheet">

</head>

<body class="gray-bg">

    <div class="middle-box text-center loginscreen animated fadeInDown">
        <div>
            <h3>Register to IN+</h3>
            <p>Create account to see it in action.</p>

            @using (Html.BeginForm("CriarAcesso", "Login", FormMethod.Post, new { @class = "m-t", role = "form" }))
            {
                @Html.AntiForgeryToken()
                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Nome, new { @class = "form-control", placeholder = "Nome" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Sobrenome, new { @class = "form-control", placeholder = "Sobrenome" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Telefone, new { @class = "form-control", placeholder = "Telefone" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Cpf, new { @class = "form-control", placeholder = "CPF" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Login.Email, new { @class = "form-control", placeholder = "Email" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Login.Senha, new { @class = "form-control", placeholder = "Senha" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Endereco.Cep, new { @class = "form-control", placeholder = "CEP" })
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Endereco.Logradouro, new { @class = "form-control", placeholder = "Rua" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Endereco.Bairro, new { @class = "form-control", placeholder = "Bairro" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Endereco.Numero, new { @class = "form-control", placeholder = "Numero" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Endereco.Complemento, new { @class = "form-control", placeholder = "Complemento" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Endereco.Cidade, new { @class = "form-control", placeholder = "Cidade" })
                        </div>
                        <div class="form-group">
                            @Html.TextBoxFor(x => x.Endereco.Estado, new { @class = "form-control", placeholder = "Estado" })
                        </div>
                    </div>
                </div>
                <button type="submit" class="btn btn-primary block full-width m-b">Registrar</button>

                <p class="text-muted text-center"><small>Already have an account?</small></p>
                <a class="btn btn-sm btn-white btn-block" href="login.html">Login</a>
            }

            <p class="m-t"> <small>Inspinia we app framework base on Bootstrap 3 &copy; 2014</small> </p>
        </div>
    </div>

    <!-- Mainly scripts -->
    <script src="~/Content/js/jquery-3.1.1.min.js"></script>
    <script src="~/Content/js/bootstrap.min.js"></script>
    <!-- iCheck -->
    <script src="~/Content/js/plugins/iCheck/icheck.min.js"></script>
    <script>
        $(document).ready(function () {
            $('.i-checks').iCheck({
                checkboxClass: 'icheckbox_square-green',
                radioClass: 'iradio_square-green',
            });
        });
    </script>
</body>


</html>
